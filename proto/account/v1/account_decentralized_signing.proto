syntax = "proto3";
package account;

// Describes the decentralized signature the user can setup to validate external resources.

message RegisterAccountSigningKeyRequest {
    string public_key = 1; 
} 

message RegisterAccountSigningKeyResponse {
    
}